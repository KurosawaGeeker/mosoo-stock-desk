{
  "name": "mosoo-stock-desk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "node --check src/worker.js && wrangler deploy --dry-run --outdir dist",
    "deploy:mosoo": "mosoo console apps deploy-app"
  },
  "devDependencies": {
    "wrangler": "^4.24.3"
  }
}
